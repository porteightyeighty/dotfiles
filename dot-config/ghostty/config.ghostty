macos-option-as-alt = left

# Let Option+Left/Right pass through as M-Left/M-Right (ESC[1;3D / ESC[1;3C)
# instead of Ghostty's default esc:b / esc:f, so tmux prefix+Option+arrow can resize panes.
keybind = alt+left=unbind
keybind = alt+right=unbind
